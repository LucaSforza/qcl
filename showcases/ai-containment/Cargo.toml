[package]
name = "ai-containment"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
description = "Executable AI containment model checked with Quantified Coalition Logic"
publish = false

[dependencies]
qcl = { path = "../.." }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
