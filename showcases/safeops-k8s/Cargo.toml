[package]
name = "safeops-k8s"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
description = "Local Kubernetes SafeOps showcase built on QCL"
publish = false

[dependencies]
qcl = { path = "../.." }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
